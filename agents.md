# Agents

- Todos os comandos e scripts deste projeto devem considerar execucao via Git Bash no Windows.
- Quando houver comando de ativacao de ambiente virtual, usar `source .venv/Scripts/activate`.
- Ao sugerir comandos interativos para este repositorio, assumir shell compatível com Bash, nao PowerShell.
